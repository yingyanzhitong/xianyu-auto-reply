# 更新日志

## 0.3.24 - 2026-08-12

- 修复鱼小铺发布时“宝贝所在地”候选只按完整地址串匹配的问题；现支持省市区顺序变化或候选省份省略，同时仍要求至少匹配市和区县以避免选错地址。
- 返佣发布继续使用原有地址选择策略；标准批量发布、返佣发布均不受本次鱼小铺修复影响。

## 0.3.23 - 2026-08-11

- 修复鱼小铺首次打开时停留在“快速进入”页导致发布表单未加载的问题；自动完成快速进入后重新打开发布页。
- 鱼小铺批量发布进度增加失败详情，展示账号、商品和后端返回的失败原因，便于后续排查。

## 0.3.22 - 2026-08-11

- 新增鱼小铺批量发布入口和独立任务编排，不影响原有批量发布流程；支持库存、包邮/按距离计费/一口价/无需邮寄、自提和三档粉丝价。
- 商品素材库新增鱼小铺可选字段，发布成功后自动设置粉丝价；粉丝价失败保留商品发布成功状态并在日志中提示告警。
- 鱼小铺素材与批量发布接口支持 `X-API-Key` 外部调用，可查询独立任务进度和逐条告警。

## 0.3.21 - 2026-08-11

- 模板配置弹窗恢复预填原始通知正文；未修改初始内容直接保存时不会覆盖服务端默认分支。
- 自动发货默认通知恢复原有买家展示格式，仅新增订单金额和购买数量，不再默认显示订单 ID。

## 0.3.20 - 2026-08-11

- 通知模板配置中的可用占位符增加中文字段说明，便于识别买家昵称、订单金额、购买数量、验证链接等字段用途。

## 0.3.19 - 2026-08-11

- 通知渠道新增可编辑的聊天消息、自动发货和账号状态正文模板，支持按渠道独立配置英文占位符并在无效时自动回退系统默认内容。
- 自动发货通知补充真实买家昵称、订单金额和购买数量；亦凡对接卡券的发货失败通知统一使用自动发货模板。

## 0.3.18 - 2026-08-10

- 修复 WebSocket 重启后人工回复丢失买家上下文的问题：系统会从消息日志恢复买家和商品维度，确保后续买家消息记录为“暂停回复”。

## 0.3.17 - 2026-08-10

- 消息日志的“暂停回复”失败原因增加精确的预计恢复时间；人工再次回复后，后续暂停记录会显示刷新后的结束时刻。

## 0.3.16 - 2026-08-10

- AI 设置新增“人工回复后暂停 AI”开关与时长配置；按账号、买家、商品维度暂停 AI，关键词和默认回复仍可继续生效。
- AI 暂停期间收到买家消息会写入消息日志，显示 AI 回复、空回复内容和黄色“暂停回复”状态，并注明暂停分钟数。
- 左侧栏系统名称右侧展示当前版本号。

## 0.3.15 - 2026-08-10

- 卡券 API 配置的 POST 请求可用参数增加 `{buyer_name}`；调用接口时会传入买家闲鱼昵称，订单详情同步保留该昵称。

## 0.3.14 - 2026-08-04

- 账号管理新增“已下单商品禁止AI回复”开关：仅当买家已下单当前咨询商品时跳过 AI；买过 A 商品后咨询 B 商品仍可使用 AI 回复。
- 移除手工录入买家 ID、商品 ID 的错误规则入口，并为账号、买家、商品的订单精确查询补充索引。

## 0.3.13 - 2026-08-04

- 账号管理的功能开关新增“禁止AI回复”规则入口，可按账号 ID、买家 ID、商品 ID 精确配置；命中规则时跳过 AI，关键词回复和默认回复保持可用。

## 0.3.12 - 2026-08-04

- 新增 `prepare-upstream-pr` 项目技能：可从指定功能点或提交提炼最小补丁，基于 `base/main` 创建分支并发起、核验正式上游 PR。

## 0.3.11 - 2026-08-04

- 修复自动回复常驻 WebSocket 被闲鱼以正常关闭码断开后直接退出的问题；现在会按既有抖动退避策略自动重连，恢复后续买家消息接收。

## 0.3.10 - 2026-08-04

- 撤销 0.3.8 在线聊天订阅改动和 0.3.9 自动回复 WebSocket 改动，仅保留 0.3.7 的订单账号隔离、重复订单过滤与创建会话失败冷却修复。

## 0.3.7 - 2026-08-03

- 修复多账号使用相同订单号时的串单问题：订单详情、手动/无物流/自动/定时发货、聊天工作台、失败原因、聊天会话和发货日志均按 `(account_id, order_no)` 定位。
- 卖家订单同步会跳过“买家即当前账号”的自购记录，订单管理同时过滤已有同类历史记录，避免小马购买鹰眼商品后出现可发货的重复订单。
- 创建会话失败不再写入 `FAILED_*` 占位会话；遗留占位会被清除并按正确账号进入冷却后重试，避免永久跳过发货。

## 0.3.6 - 2026-08-03

- 自动求小红花改为花朵与卡车两个互斥模式：选择花朵时按原有范围执行，选择卡车时自动启用求小红花且仅处理已发货、已完成订单。
- 服务端同时更新两个模式字段，避免重复开启；订单未发货时不再因卡车模式触发求小红花，减少对买家的打扰。

## 0.3.4 - 2026-08-03

- 回滚 0.3.3 的订单账号隔离改动，恢复此前稳定的订单查询、会话创建和自动发货流程。

## 0.3.3 - 2026-08-03

- 修复多账号订单隔离：订单同步会跳过“买家即当前账号”的自购记录；创建、查询、状态更新、自动/手动/定时发货、聊天工作台和自动发货日志均按 `(account_id, order_no)` 定位，避免同一订单号跨账号串单。

## 0.3.2 - 2026-08-03

- GitHub Actions 部署在全部健康检查通过后自动清理未被任何容器引用的旧 Docker 镜像，避免历史镜像持续占用服务器磁盘；容器、数据卷和当前运行镜像不会被删除。

## 0.3.1 - 2026-08-03

- 合入账号商品隔离与 API Key 明文展示修复：商品管理按账号数据库主键和所属用户精确查询，前端切换账号时丢弃过期请求；管理员可查看经运行期密钥加密保存的 API Key。

## 0.3.0 - 2026-08-03

- 基于 `zhinianboke/xianyu-auto-reply` 最新主分支同步上游更新，并保留 Gamer520 素材同步、卡券商品标题变量、线程保护与 GitHub Actions 部署能力。
- 发布镜像标签升级为 `0.3.0`，避免复用此前已发布的 `v0.2.9` 标签。

## 0.2.9 - 2026-07-31

- 将生产 MySQL root 密码、MySQL 应用密码、Redis 密码和卡券上游 `EXTERNAL_API_KEY` 迁移为 GitHub Repository Secrets；Actions 通过 Base64 编码的 SSH 标准输入写入服务器 `.env`，不会在日志或仓库中输出明文。
- GitHub Actions 远端部署钩子只接受四个允许字段，拒绝空值、换行符和未授权字段；写入 `.env` 后设置为仅当前用户可读。
- 移除 `docker-compose.yml`、本地/远程部署脚本、更新脚本和环境变量示例中的硬编码密码及外部 API Key。首次本地部署会生成随机 MySQL/Redis 密码，外部 API Key 必须显式配置。

## 0.2.8 - 2026-07-30

- 新增 GitHub Actions 生产发布流水线：版本标签或手动触发后并行构建 Backend、WebSocket、Scheduler 和 Frontend 的 `linux/amd64` 镜像并推送 GHCR。
- 新增受限 SSH 部署脚本和生产 Compose 覆盖文件；部署前自动备份 MySQL，只重建四个应用容器，并验证容器与公网健康状态。
- Actions 仅使用短期 `GITHUB_TOKEN` 登录 GHCR，普通分支和 Pull Request 不触发生产部署，服务器专用密钥不能执行任意 Shell 命令。

## 0.2.5 - 2026-07-29

- 卡券 API 配置的“POST 请求可用参数”增加 `item_title` 快捷变量；自动发货、手动发货和提货请求均支持将 `{item_title}` 替换为真实商品标题，并在商品目录尚未采集到外部发布商品时回退使用卡券关联记录保存的标题。

## 0.2.4 - 2026-07-29

- 卡券批量绑定接口增加可选 `item_title` 参数并持久化到商品关联记录；卡券管理在商品目录尚未采集到该商品时，也能回显外部发布时传入的标题。

## 0.2.3 - 2026-07-28

- Gamer520 外部素材首次写入时增加同用户同名检查，兼容有无 `【秒发】` 前缀；已存在同名素材时返回 `skipped`，不重复创建或发布。

## 0.2.1 - 2026-07-28

- 修复闲鱼首页和发布页存在持续网络连接时，Playwright 等待 `networkidle` 导致商品发布固定超时的问题。

## 0.2.0 - 2026-07-28

- 为每个用户增加可生成、重置和撤销的 API Key，仅保存 SHA-256 摘要与脱敏信息；REST API 支持与所属用户 JWT 等价的 `X-API-Key` 鉴权。
- 商品素材支持外部来源标识和内容哈希，新增最多 20 条的幂等批量写入接口，防止 Gamer520 重复创建素材。
- 批量发布增加持久化批次、UUID `request_id` 幂等控制、账号归属校验，以及包含逐商品结果的状态查询接口。
- 远程发布图片增加公网地址、图片类型和 10MB 大小校验，拒绝内网、回环及不受支持的资源。
- 用户管理页面增加 API Key 生成、一次性复制、重置和撤销操作。

## 0.1.3 - 2026-07-27

- 修复 API 卡券仅将 HTTP 200 视为成功的问题，兼容令牌服务返回的 `201 Created`，避免已生成兑换码未发送时被重复下发。

## 0.1.2 - 2026-07-27

- 增加 WebSocket 服务的生产部署覆盖配置，固定使用包含线程保护的自定义镜像。

## 0.1.1 - 2026-07-27

- 限制同步数据库兼容层的活跃线程数，避免超时任务耗尽容器 PID 配额。
- Cookie 刷新失败改为指数退避，避免滑块验证失败后短间隔反复触发。
- 浏览器任务在线程池提交前限流，并确保取消时等待实际任务完成后才释放名额。
